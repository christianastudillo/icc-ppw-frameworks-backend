info:
  name: Error2
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8080/api/products/page?size=500
  params:
    - name: size
      value: "500"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
