info:
  name: Error3
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:8080/api/products/page?sortBy=owner.name
  params:
    - name: sortBy
      value: owner.name
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
