info:
  name: EndpointSINToken
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/products/page?page=0&size=5
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
