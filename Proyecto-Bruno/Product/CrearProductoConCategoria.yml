info:
  name: CrearProductoConCategoria
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/api/products
  body:
    type: json
    data: |-
      {
        "name": "Laptop Gaming",
        "price": 1200.0,
        "stock": 10,
        "userId": 1,
        "categoryId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
