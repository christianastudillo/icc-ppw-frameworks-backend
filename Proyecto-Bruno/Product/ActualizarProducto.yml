info:
  name: ActualizarProducto
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/api/products/1
  body:
    type: json
    data: '{ "name": "Notebook", "price": 1200.00, "stock": 10 }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
