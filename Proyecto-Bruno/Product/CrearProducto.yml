info:
  name: CrearProducto
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/products
  body:
    type: json
    data: |
      {
        "name": "Mouse",
        "price": 20.0,
        "stock": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
