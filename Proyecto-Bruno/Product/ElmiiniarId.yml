info:
  name: ElmiiniarId
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:8080/api/products/5
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
