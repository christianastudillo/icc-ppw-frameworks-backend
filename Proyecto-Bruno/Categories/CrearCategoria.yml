info:
  name: CrearCategoria
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/categories
  body:
    type: json
    data: |
      {
        "name": "Computadoras",
        "description": "Equipos de cómputo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
