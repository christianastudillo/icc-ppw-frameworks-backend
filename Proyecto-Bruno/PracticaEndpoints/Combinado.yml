info:
  name: Combinado
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/users/1/products?name=laptop&minPrice=500&categoryId=2
  params:
    - name: name
      value: laptop
      type: query
    - name: minPrice
      value: "500"
      type: query
    - name: categoryId
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
