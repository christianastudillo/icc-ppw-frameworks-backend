info:
  name: ParametroNegativo
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:8080/api/users/1/products?minPrice=-5
  params:
    - name: minPrice
      value: "-5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
