info:
  name: FiltroCategoria
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/users/1/products?categoryId=3
  params:
    - name: categoryId
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
