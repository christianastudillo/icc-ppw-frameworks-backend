info:
  name: RangoPrecio
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/users/1/products?minPrice=500&maxPrice=1500
  params:
    - name: minPrice
      value: "500"
      type: query
    - name: maxPrice
      value: "1500"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
