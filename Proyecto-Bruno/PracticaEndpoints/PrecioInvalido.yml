info:
  name: PrecioInvalido
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/users/1/products?minPrice=1500&maxPrice=500
  params:
    - name: minPrice
      value: "1500"
      type: query
    - name: maxPrice
      value: "500"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
