info:
  name: ProductosDelUsuario
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8080/api/users/1/products
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
